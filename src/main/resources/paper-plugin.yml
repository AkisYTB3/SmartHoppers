name: SmartHoppers
version: ${version}
main: org.notionsmp.smarthoppers.SmartHoppers
api-version: '1.21'
load: POSTWORLD
author: "Akis (discord: yeahakis)"
dependencies:
  server:
    Nexo:
      load: BEFORE
      required: false
    CrashClaim:
      load: BEFORE
      required: false
      join-classpath: true
    HuskTowns:
      load: BEFORE
      required: false
      join-classpath: true
    WorldGuard:
      load: BEFORE
      required: false
      join-classpath: true
    Towny:
      load: BEFORE
      required: false
      join-classpath: true
    Factions:
      load: BEFORE
      required: false
      join-classpath: true
    Lands:
      load: BEFORE
      required: false
      join-classpath: true
    PlotSquared:
      load: BEFORE
      required: false
      join-classpath: true